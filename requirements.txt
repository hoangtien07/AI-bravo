# --- Lõi RAG ---
chromadb>=0.5.0
pyyaml>=6.0
rank-bm25>=0.2.2          # BM25 cho hybrid search
tiktoken>=0.7            # đếm token THẬT để ước chi phí (không bịa)
python-dotenv>=1.0       # nạp API key từ .env

# --- Nhà cung cấp model (chỉ cần cài cái bạn dùng) ---
google-genai>=0.3        # Gemini (cloud) — mặc định
openai>=1.40             # OpenAI (cloud) — tuỳ chọn
ollama>=0.3.0            # self-host (fallback / đối chứng chủ quyền dữ liệu)

# --- UI + crawl ---
streamlit>=1.36
requests>=2.31
beautifulsoup4>=4.12
lxml>=5.0
playwright>=1.40         # crawl SPA help.bravo.com.vn (cần: playwright install chromium)

# --- Rerank tuỳ chọn (đặt RERANK_ENABLED=1; model bge-reranker-v2-m3 ~600MB) ---
# sentence-transformers>=3.0
